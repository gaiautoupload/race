const esc=v=>String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"})[c]);
const money=v=>v?`約 ${(Number(v)/10000).toFixed(0)} 萬`:"—";
const stats=s=>!s.has_data?`<div class="no-period-data">此期間沒有可觀測分點成交資料</div>`:`<div class="stat-grid"><div class="buy-stat"><small>買進金額</small><b>${esc(s.buy_amount_label||money(s.buy_amount))}</b><span>${Number(s.buy_lots||0).toLocaleString()} 張 · 加權均價 ${Number(s.buy_avg_price||0).toFixed(2)}</span></div><div class="sell-stat"><small>賣出金額</small><b>${esc(s.sell_amount_label||money(s.sell_amount))}</b><span>${Number(s.sell_lots||0).toLocaleString()} 張 · 加權均價 ${Number(s.sell_avg_price||0).toFixed(2)}</span></div><div><small>淨流向</small><b class="${s.net_amount>0?"gain":s.net_amount<0?"loss":"flat"}">${s.net_amount>0?"+":""}${money(s.net_amount)}</b><span>${s.net_amount>0?"偏多掃貨":s.net_amount<0?"偏空出貨":"持平"}</span></div></div>`;
const flowTable=(rows,side)=>`<table class="flow-table"><thead><tr><th>#</th><th>個股</th><th>金額</th><th>張數</th><th>加權均價</th></tr></thead><tbody>${rows.length?rows.map((x,i)=>`<tr class="${side}"><td>${x.rank||i+1}</td><td><strong>${esc(x.stock_code)}</strong> ${esc(x.stock_name)}</td><td>${esc(x.amount_label)}<small class="wan">${Number(x.amount_wan||0).toLocaleString()} 萬</small></td><td>${Number(x.lots).toLocaleString()} 張</td><td>${Number(x.avg_price||0).toFixed(2)}</td></tr>`).join(""):`<tr><td colspan="5">此期間沒有可觀測成交資料</td></tr>`}</tbody></table>`;
const spotlight=(title,rows,kind)=>`<section class="spotlight ${kind}"><div><p>${kind==="buy"?"TOP BUY · 大量掃貨":"TOP SELL · 大量出貨"}</p><h2>${title}</h2></div>${rows.length?`<div class="spot-items">${rows.slice(0,3).map((x,i)=>`<div><em>${i+1}</em><b>${esc(x.stock_code)} ${esc(x.stock_name)}</b><span>${esc(x.amount_label)} · ${Number(x.lots).toLocaleString()} 張</span></div>`).join("")}</div>`:`<div class="empty">目前沒有大額紀錄</div>`}</section>`;
function chart(points){if(!points?.length)return `<div class="empty chart-empty">尚無累計曲線資料</div>`;const w=900,h=220,p=18,vals=points.map(x=>Number(x.value)),min=Math.min(...vals,0),max=Math.max(...vals,0),range=max-min||1;const xy=(v,i)=>`${p+i*(w-2*p)/(points.length-1||1)},${h-p-(v-min)*(h-2*p)/range}`;const line=points.map((x,i)=>xy(x.value,i)).join(" ");const zero=h-p-(0-min)*(h-2*p)/range;return `<svg class="equity-chart" viewBox="0 0 ${w} ${h}" role="img" aria-label="累計資產損益推估折線圖"><line x1="${p}" y1="${zero}" x2="${w-p}" y2="${zero}" class="zero"/><polyline points="${line}"/><circle cx="${w-p}" cy="${xy(vals.at(-1),vals.length-1).split(",")[1]}" r="5"/><text x="${p}" y="${h-2}">${esc(points[0].date)}</text><text x="${w-p}" y="${h-2}" text-anchor="end">${esc(points.at(-1).date)}</text></svg><div class="chart-caption">累計淨流向推估：買進為資金流出、賣出為資金流入；僅作主力活躍度參考。</div>`}
const code=new URLSearchParams(location.search).get("code");fetch("../public/data/dashboard.json",{cache:"no-store"}).then(r=>r.json()).then(d=>{const p=d.players[code];if(!p){document.querySelector("#player").textContent="找不到選手資料，請由排行榜重新選擇。";return}const t=p.today,f=p.five_days;document.querySelector("#asof").textContent=`行情日：${p.market_date||"—"} · 最近分點資料：${p.latest_flow_date}`;document.querySelector("#player").innerHTML=`<section class="player-hero"><a href="../">← 返回競技場</a><p>PLAYER DOSSIER</p><h1>${esc(p.alias)}</h1><span>匿名選手 · 單日／五日／歷史戰績</span><div>最新行情與分點成交可能不同日；頁面依最新可觀測資料呈現。</div></section><section class="spotlight-grid">${spotlight("推薦買入",t.recommended_buy||t.buys,"buy")}${spotlight("建議賣出",t.recommended_sell||t.sells,"sell")}</section><section class="period"><div class="period-head"><p>TODAY · ${esc(p.latest_flow_date)}</p><h2>今日戰績</h2></div>${stats(t.stats||{})}<div class="split"><section class="flow-card buy"><h3>單日買進排行</h3>${flowTable(t.buys,"buy")}</section><section class="flow-card sell"><h3>單日賣出排行</h3>${flowTable(t.sells,"sell")}</section></div></section><section class="period"><div class="period-head"><p>LAST 5 TRADING DAYS</p><h2>五日戰績</h2></div>${stats(f.stats||{})}<div class="split"><section class="flow-card buy"><h3>五日買進排行</h3>${flowTable(f.buys,"buy")}</section><section class="flow-card sell"><h3>五日賣出排行</h3>${flowTable(f.sells,"sell")}</section></div></section><section class="equity"><p>EQUITY CURVE · ESTIMATE</p><h2>資產損益累加推估</h2>${chart(p.equity_curve)}</section><section class="holdings"><p>HISTORICAL INVENTORY · TOP 10</p><h2>歷史推估持股</h2><table><thead><tr><th>個股</th><th>最早買入</th><th>最新買入</th><th>推估張數</th><th>平均成本（萬／張）</th><th>最新價</th><th>推估報酬</th></tr></thead><tbody>${p.holdings.length?p.holdings.map(x=>`<tr><td>${esc(x.stock_code)} ${esc(x.stock_name)}</td><td>${esc(x.first_buy_date||"—")}</td><td>${esc(x.latest_buy_date||"—")}</td><td>${Number(x.lots).toLocaleString()} 張</td><td>${Number(x.average_cost_wan_per_lot).toFixed(2)}</td><td>${Number(x.latest_price).toFixed(2)}</td><td class="${x.roi_pct>=0?"gain":"loss"}">${x.roi_pct>=0?"+":""}${Number(x.roi_pct).toFixed(1)}%</td></tr>`).join(""):`<tr><td colspan="7">尚無可用的正向推估庫存。</td></tr>`}</tbody></table></section><section class="copy"><p>COPY-TRADING PLAYBOOK · RESEARCH ONLY</p><h2>如何設計跟單</h2><ol><li><b>進場觀察</b><span>${esc(p.copy_plan.entry)}</span></li><li><b>部位控制</b><span>${esc(p.copy_plan.sizing)}</span></li><li><b>退場紀律</b><span>${esc(p.copy_plan.exit)}</span></li></ol><footer>${esc(p.copy_plan.guardrail)}<br>${esc(d.notice)}</footer></section>`}).catch(()=>document.querySelector("#player").textContent="資料暫時無法載入。");
